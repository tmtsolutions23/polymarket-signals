# Polymarket Scanner — Log

Running log of what happened (charter §7.1). Newest first.

- 2026-09-20 — HERMES-011 — Kalshi smart-money scanner v2 live (cron f339f30bf87f, 07:00/19:00 UTC → #midas, CEO "go"): leaderboards → 5 visible whales (of 158) → 7-day trade histories → fresh-buy consensus, single-whale moves, **current holdings** (buy/sell netting, maker+taker, NO-side effective price = 1−yesP). Live signals: jake6767 holds ~$10.5k YES Bills -10; szg.szg trading BTC 15-min ranges $17k-30k. Doc: HERMES-011.md. Key structural finding: Kalshi opt-in sharing ⇒ single-whale/hold venue, not consensus venue (Polymarket stays the consensus play).
- 2026-09-18 — (research, CEO request) — Kalshi vs Polymarket for copy-trading: verdict **stay on Polymarket**. Kalshi volume is ~10x the CEO's tradable market (Polymarket US) but has ZERO public trader identity (OpenAPI: 98 endpoints, no leaderboard, anonymous trade feed only) — the consensus strategy cannot transfer. Fees are a wash (same taker curve on both US exchanges). Optional P3: Kalshi block-trade flow scanner as supplementary signal. No hire needed. Doc: `Research-Kalshi-vs-Polymarket.md`.
- 2026-09-18 — HERMES-010 — Hourly cadence + change gate (CEO-approved deploy): cron `0 7,18 * * *` → `0 * * * *`; new `Change Gate (new signals)` node between US Availability Gate and Prompt & Route (≥$10k filter + dedupe vs `/root/polymarket-n8n/last_posted.json`, fail-open on state errors, drift re-post requires >2% change AND ≥90 min gap); `Silent (no signal)` replaces both IF skip branches — filler posts removed; workflow renamed "Hourly NY" (version 88d947f9, counter 68). Verified: activated, webhook runs 37/38 success (silent no-signal), gate unit-tested (post → silent → new-only). Watcher missed-slot logic auto-adapts to hourly.
- 2026-09-17 — HERMES-009 — Midas instantiated (CEO-approved contract watch, 7 days): #midas channel + webhook, Agent Spec → #agent-registry, card `30-Agents/Midas-PolymarketAnalyst.md`, digest cron `8de94bad3df4` daily 23:30 UTC → #midas. Expiry 2026-09-25 → archive + revoke + stop cron. Purpose: verify the new pre-filter/settle-gate on live days and deliver the first official weekly scorecard (~Sep 24).
- 2026-09-17 — HERMES-008 + F4 pre-filter — Deploy bundle v2 (CEO-approved): F3 settle-gate fix — verification now rejects settled/ended markets (`settleDays<=0` OR `closed` OR `active=false`); F4 noise pre-filter — only ≥$10k smart-money picks reach the report (live-verified: test run posted "No signals above $10k smart-money this run." 04:39 UTC); workflow name synced to "07:00+18:00 NY" (version 30771862, counter 57). Deployed via DB + `workflow_history` (CLI import blocked by gateway guard); activated + webhook-tested (run 29 success). Scorecard logging stays single-writer: exec-parse suite owns `picks_history.jsonl`; no in-workflow node.
- 2026-09-17 — (ops) — Bankroll $100 → $96.75 (CEO took the Sonmez COPY stake). First scorecard data point: that COPY lost; 3/3 settled SKIPs won (gate may be too strict — review at weekly).
- 2026-09-17 — HERMES-003 — Outcome tracker live: `picks_history.jsonl` (append-only, 19 runs / 309 picks backfilled from n8n execution DB), gamma resolver (58 settled via `/events?slug=`; `/markets` drops closed markets — don't use for settlement), weekly scorecard (COPY win rate, WATCH→winner, SKIP false-negative; gate ≥7 scheduled-rated days; interim posted to #c-suite). Cron `poly-scorecard-daily` (085eaa90453c) daily 23:05 UTC. First data points: scheduled COPY Sonmez @0.383 LOST; 3/3 settled SKIPs won anyway. Guarded n8n change (in-workflow pick recording) spec'd for CEO review, not deployed. Docs: HERMES-003.md, scorecard/README.md, Rollup (## Scorecard).
- 2026-09-17 — HERMES-005 — Failure alerting live: watcher `n8n_failure_watcher.py` (cron `353e5ceabcbd`, every 5 min) checks the local n8n execution DB for failed runs of `4MqMruEVp1YutQf7` and posts to #polymarket-daily via the pipeline's own webhook (fallback #blockers; #blockers webhook added to webhooks.json). Error message extracted from execution_data (pointer-traversal). Missed-slot detection with schedule-change guards (verified via workflow_history: cron was `0 7 * * *` until 2026-09-16 04:56 UTC). Standup digest now carries a Scanner health line (state written after post — old silent-watchdog trap fixed). Tested end-to-end: induced guard rejection (exec #27) → alert observed in #polymarket-daily at 04:32:27 UTC; re-run quiet; clarifying note posted 04:35:46 UTC; test execution removed from DB. Docs: HERMES-005.md, Brief (risks/cadence), Rollup (metrics).
- 2026-09-17 — HERMES-004 — Cadence audit: CEO confirmed 2x/day (07:00+18:00 New York, instance tz `America/New_York`; "approved" in #ceo-office 04:11 UTC). Workflow `4MqMruEVp1YutQf7` renamed "Polymarket Smart Money Consensus — Daily 7AM" → "07:00+18:00 NY" (metadata-only, DB direct with backup; versionId/active untouched). Brief + Rollup cadence lines corrected London → New York. Done.
- 2026-09-17 — HERMES-002 — Fold-in: project folder created (Brief/Log/Rollup), D-005 recorded, ownership + cadence mapped to org (Atlas DRI), change control defined, iteration tickets HERMES-003..006 created on the board. Pipeline itself untouched (deploy is guarded). Discord webhooks for ceo-office/kanban/decisions added to scaffold + webhooks.json (7 channels).
